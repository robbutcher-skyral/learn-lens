/** Replace: ensure PLUGIN_ID is unique across plugins. */
export const PLUGIN_ID = "dashboard-plugin";

/** Replace: add more layer IDs if your plugin registers multiple layers. */
export const LAYER_IDS = {
  MAIN: "dashboard-main",
} as const;
